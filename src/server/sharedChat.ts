import {Express} from 'express';import {db} from '../db';import {shoutboxMessages} from '../db/schema';import {desc,gte} from 'drizzle-orm';
export function chatDayStart(now=new Date()){
 const parts=Object.fromEntries(new Intl.DateTimeFormat('en-GB',{timeZone:'Europe/Stockholm',year:'numeric',month:'2-digit',day:'2-digit',hour:'2-digit',hourCycle:'h23'}).formatToParts(now).map(p=>[p.type,p.value]));
 const noon=new Date(`${parts.year}-${parts.month}-${parts.day}T12:00:00Z`);if(Number(parts.hour)<4)noon.setUTCDate(noon.getUTCDate()-1);const localHour=Number(new Intl.DateTimeFormat('en-GB',{timeZone:'Europe/Stockholm',hour:'2-digit',hourCycle:'h23'}).format(noon));const start=new Date(noon);start.setUTCHours(4-(localHour-12),0,0,0);return start;
}
export function registerSharedChat(app:Express){app.get('/api/shoutbox',async(_req,res)=>{try{res.json(await db.select().from(shoutboxMessages).where(gte(shoutboxMessages.createdAt,chatDayStart())).orderBy(desc(shoutboxMessages.createdAt)).limit(500))}catch{res.status(500).json({error:'Chatten kunde inte hämtas.'})}});app.post('/api/shoutbox',async(req:any,res)=>{try{const content=String(req.body.content||'').trim();if(!content||content.length>500)return res.status(400).json({error:'Skriv ett meddelande med högst 500 tecken.'});if(!req.arenaUser)return res.status(401).json({error:'Logga in för att skriva.'});const [message]=await db.insert(shoutboxMessages).values({userId:req.arenaUser.userId,authorName:req.arenaUser.name,content}).returning();res.json(message)}catch{res.status(500).json({error:'Meddelandet kunde inte sparas.'})}});}
