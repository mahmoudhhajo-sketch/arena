import {useState,useEffect} from 'react';import {WorldView} from './WorldViews';import {SeriesView} from './SeriesView';import {Statistics} from './StatisticsPage';import {RulesView,BeginnerGuide} from './ManualViews';import {EntityPage} from './EntityPage';import {Entity} from './FeatureViews';import {api} from '../lib/api';import {MatchView} from './RetroMatchView';
export function GuestGame(){const [tab,setTab]=useState('Serier'),[entity,setEntity]=useState<Entity|null>(null),[match,setMatch]=useState<any>(null),[error,setError]=useState('');useEffect(()=>{const open=(e:any)=>setEntity(e.detail);window.addEventListener('arena-entity',open);return()=>window.removeEventListener('arena-entity',open)},[]);return <main className="guest-shell"><header className="masthead"><h1 className="wordmark">ARENA</h1><p>Gäst i Mambenna · Läs och upptäck spelvärlden</p><button onClick={async()=>{await api('/auth/logout',{});localStorage.removeItem('arena_guest');location.reload()}}>Till inloggningen</button></header><nav className="tabs">{['Serier','Världen','Statistik','Nybörjarguide','Regler'].map(t=><button key={t} aria-selected={tab===t} onClick={()=>{setTab(t);setEntity(null);setMatch(null)}}>{t}</button>)}</nav><section className="game-page">{error&&<p>{error}</p>}{entity?<EntityPage entity={entity} viewerUserId="guest" onBack={()=>setEntity(null)} onRefresh={()=>{}}/>:match?<><button onClick={()=>setMatch(null)}>← Tillbaka</button><MatchView match={match} matchesHistory={[match]} onSelectMatch={()=>{}} onSimulateNewMatch={()=>{}}/></>:tab==='Världen'?<WorldView/>:tab==='Statistik'?<Statistics onOpen={setEntity}/>:tab==='Regler'?<RulesView/>:tab==='Nybörjarguide'?<BeginnerGuide/>:<SeriesView userClub={{id:'guest'} as any} onClub={id=>setEntity({kind:'club',id})} onMatch={async id=>{try{const m=await api('/match/'+id);if(m.upcoming)setError('Den här matchen är ännu inte spelad.');else{setMatch(m);setError('')}}catch(e:any){setError(e.message)}}}/>}</section></main>}
